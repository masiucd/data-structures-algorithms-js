# Points

## Description

Given array with scores find t=out the total score. If x > y then x gets 3 points, if x < y then y gets 3 points, if x = y then both get 1 point.

## Examples

```ts
points([1, 2], [2, 1]); // [3, 3]
points([1, 2], [1, 2]); // [1, 1]
points([1, 2], [1, 3]); // [1, 3]
```
